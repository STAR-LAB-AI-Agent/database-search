# 复制本文件为 .env，按需填写（均为可选）。

# DeepSeek API Key —— 由 nanobot 使用（nanobot/setup.py 会读取它并写入 ~/.nanobot/config.json）。
# 本项目 CLI 本身不再调用 DeepSeek；问答/编排全部交给 nanobot。
# 申请地址：https://platform.deepseek.com
DEEPSEEK_API_KEY=sk-xxxx

# HuggingFace 镜像端点（国内下载 BGE 模型时用，可选）
HF_ENDPOINT=https://hf-mirror.com
